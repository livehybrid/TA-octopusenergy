#
# Runtime dependencies vendored into package/lib/ by ucc-gen at build time.
#
# Versions are the newest that stay compatible with Splunk's Python 3.9 LTS
# runtime (the pin target), NOT simply "latest":
#   - splunktaucclib >= 8.2.0 : floor required by the current ucc-gen (>= 6.5.2);
#     8.2.0 still ships splunk_aoblib / BaseModInput, which these collectors use.
#   - requests < 2.33.0 : splunktaucclib's send_http_request uses requests;
#     2.33.0+ needs Python >= 3.10 and 2.34.0 uses runtime `str | bytes` unions
#     that raise TypeError at import on 3.9.
#   - splunk-sdk < 3.0.0 : 3.0.0 is Python >= 3.13 only.
#
splunktaucclib>=8.2.0
solnlib>=5.5.0
splunk-sdk>=2.0.2,<3.0.0
requests>=2.32.0,<2.33.0
pyjwt
